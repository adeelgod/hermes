(function(module) {
    'use strict';

    try {
        module = angular.module('hermes.crm.controller');
    } catch (e) {
        module = angular.module('hermes.crm.controller', []);
    }

    module.controller('FooterCtrl', function ($scope) {
        // TODO: implement this
    });
})();
